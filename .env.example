# ============================================
# DIRECTUS CRM - Variables de Entorno
# ============================================
# Copia este archivo a .env y completa los valores

# --- Claves de Seguridad (OBLIGATORIO) ---
# Genera claves únicas con: openssl rand -hex 32
DIRECTUS_KEY=tu-clave-secreta-aqui-genera-con-openssl
DIRECTUS_SECRET=tu-secreto-aqui-genera-con-openssl

# --- Base de Datos PostgreSQL ---
DB_HOST=postgres
DB_PORT=5432
DB_DATABASE=crm_database
DB_USER=postgres
DB_PASSWORD=tu_password_seguro

# --- Credenciales Admin Inicial ---
ADMIN_EMAIL=admin@tuempresa.com
ADMIN_PASSWORD=admin_password_seguro

# --- URL Pública ---
# URL donde estará accesible Directus (para CORS, emails, etc.)
PUBLIC_URL=http://localhost:8055

# --- Cotizador (Módulo Integrado) ---
# URL interna del cotizador (se configura automáticamente en Docker)
COTIZADOR_EXTERNAL_URL=http://cotizador-web:5173

# --- Configuraciones Opcionales ---
# LOG_LEVEL=info
# CACHE_ENABLED=true
# CACHE_TTL=5m
# RATE_LIMITER_ENABLED=true
# RATE_LIMITER_POINTS=50
# RATE_LIMITER_DURATION=1

# --- Para Coolify (Producción) ---
# Estas variables se configuran en el panel de Coolify
# PUBLIC_URL=https://crm.tudominio.com
